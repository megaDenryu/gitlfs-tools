//! アップロード操作の結果を表すドメインモデル。

/// アップロード操作の結果。判断5「保管先のオブジェクトは変更不能として扱う」により、
/// 既に同じ内容が存在する場合は転送せず`既に存在していた`を返す。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum アップロード結果 {
    転送した,
    既に存在していた,
}
