//! 受入試験が使う試験ファイルの内容を作る純粋関数。依存も副作用も持たないため自由関数
//! として置く。乱数crateを足さない代わりに、位置と種から決まる決定的な内容にする。

pub fn 決定的な内容を作る(サイズ: usize, 種: u8) -> Vec<u8> {
    let 種 = usize::from(種);
    (0..サイズ).map(|位置| u8::try_from((位置.wrapping_add(種 * 97)) % 251).unwrap_or(0)).collect()
}
